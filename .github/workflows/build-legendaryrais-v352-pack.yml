name: Build LegendaryRais Java Pack v3.5.2 Reforged Hotfix

on:
  push:
    branches: [main]
    paths:
      - ".build/build_v352_java_pack.py"
      - ".build/trigger-v352-pack.txt"
      - ".github/workflows/build-legendaryrais-v352-pack.yml"

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install pillow
      - name: Build refined Java pack
        run: python .build/build_v352_java_pack.py
      - name: Verify
        run: |
          unzip -t LegendaryRais-Java-26.1.2-v3.5.2-REFORGED-HOTFIX.zip
          test -s LegendaryRais-Java-26.1.2-v3.5.2-REFORGED-HOTFIX.sha1
          unzip -l LegendaryRais-Java-26.1.2-v3.5.2-REFORGED-HOTFIX.zip | grep -q 'legendaryv34/items/stormpiercer_recurve_bow.json'
          unzip -l LegendaryRais-Java-26.1.2-v3.5.2-REFORGED-HOTFIX.zip | grep -q 'soultide'
      - uses: actions/upload-artifact@v4
        with:
          name: LegendaryRais-Java-26.1.2-v3.5.2-REFORGED-HOTFIX
          path: |
            LegendaryRais-Java-26.1.2-v3.5.2-REFORGED-HOTFIX.zip
            LegendaryRais-Java-26.1.2-v3.5.2-REFORGED-HOTFIX.sha1
          retention-days: 7
      - name: Publish
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add LegendaryRais-Java-26.1.2-v3.5.2-REFORGED-HOTFIX.zip LegendaryRais-Java-26.1.2-v3.5.2-REFORGED-HOTFIX.sha1
          git commit -m "Publish LegendaryRais Java v3.5.2 Reforged Hotfix pack" || exit 0
          git reset --hard HEAD
          git clean -fd
          git pull --rebase origin main
          git push
