name: Build LegendaryRais v3.4 Ultimate Arsenal
on:
  push:
    branches: [main]
    paths:
      - ".build/extracted-v340/**"
      - ".build/trigger-v340-build.txt"
      - ".github/workflows/build-legendaryrais-v340.yml"
permissions:
  contents: write
jobs:
  build-test:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '26'
      - name: Reconstruct base and apply v3.4
        run: |
          set -euo pipefail
          rm -rf .build/v340-src .build/v340-source.zip
          cat .build/v300x-00.b64 .build/v300x-01.b64 .build/v300x-02.b64 .build/v300x-03.b64 .build/v300x-04.b64 .build/v300x-05.b64 | tr -d '\n\r ' | base64 -d > .build/v340-source.zip
          unzip -t .build/v340-source.zip
          mkdir -p .build/v340-src
          unzip -q .build/v340-source.zip -d .build/v340-src
          cp .build/extracted-v340/LegendaryRais.java .build/v340-src/src/main/java/id/raisnet/legendaryrais/LegendaryRais.java
          cp .build/extracted-v340/ArsenalExpansion.java .build/v340-src/src/main/java/id/raisnet/legendaryrais/ArsenalExpansion.java
          cp .build/extracted-v340/plugin.yml .build/v340-src/src/main/resources/plugin.yml
          cp .build/extracted-v340/pom.xml .build/v340-src/pom.xml
      - name: Guard premium Water Relics unchanged
        run: |
          grep -q 'getConfig().set("base-hit.sword", 38.0)' .build/v340-src/src/main/java/id/raisnet/legendaryrais/LegendaryRais.java
          grep -q 'getConfig().set("base-hit.trident", 44.0)' .build/v340-src/src/main/java/id/raisnet/legendaryrais/LegendaryRais.java
          grep -q 'getConfig().set("abyssal-step.damage", 650.0)' .build/v340-src/src/main/java/id/raisnet/legendaryrais/LegendaryRais.java
          grep -q 'getConfig().set("leviathan.ultimate.collapse-damage", 1000.0)' .build/v340-src/src/main/java/id/raisnet/legendaryrais/LegendaryRais.java
          grep -q 'SOUL_TIDE_MODEL_DATA = 910041' .build/v340-src/src/main/java/id/raisnet/legendaryrais/LegendaryRais.java
          grep -q 'LEVIATHAN_MODEL_DATA = 910042' .build/v340-src/src/main/java/id/raisnet/legendaryrais/LegendaryRais.java
      - name: Compile
        run: |
          cd .build/v340-src
          mvn -B -U clean package
          cp target/legendaryrais-3.4.0.jar ../../LegendaryRais-3.4.0-ULTIMATE-ARSENAL-PAPER-26.1.2.jar
          sha256sum ../../LegendaryRais-3.4.0-ULTIMATE-ARSENAL-PAPER-26.1.2.jar > ../../LegendaryRais-3.4.0-ULTIMATE-ARSENAL-PAPER-26.1.2.sha256
      - name: Verify JAR
        run: |
          unzip -t LegendaryRais-3.4.0-ULTIMATE-ARSENAL-PAPER-26.1.2.jar
          unzip -p LegendaryRais-3.4.0-ULTIMATE-ARSENAL-PAPER-26.1.2.jar plugin.yml | grep -q "version: 3.4.0"
          jar tf LegendaryRais-3.4.0-ULTIMATE-ARSENAL-PAPER-26.1.2.jar | grep -q 'ArsenalExpansion.class'
      - name: Smoke boot Paper 26.1.2 build 74
        run: |
          set -euo pipefail
          UA="RaisNet-LegendaryRais-v340"
          BUILDS=$(curl -fsSL -H "User-Agent: $UA" https://fill.papermc.io/v3/projects/paper/versions/26.1.2/builds)
          URL=$(echo "$BUILDS" | jq -r 'first(.[] | select((.id == 74) or (.number == 74)) | .downloads."server:default".url) // empty')
          test -n "$URL"
          mkdir -p .build/smoke-v340/plugins
          curl -fsSL -H "User-Agent: $UA" "$URL" -o .build/smoke-v340/paper.jar
          cp LegendaryRais-3.4.0-ULTIMATE-ARSENAL-PAPER-26.1.2.jar .build/smoke-v340/plugins/
          echo "eula=true" > .build/smoke-v340/eula.txt
          cat > .build/smoke-v340/server.properties <<'EOF'
          online-mode=false
          server-port=25565
          view-distance=2
          simulation-distance=2
          max-players=2
          spawn-protection=0
          enable-query=false
          enable-rcon=false
          require-resource-pack=false
          EOF
          cd .build/smoke-v340
          set +e
          java -Xms512M -Xmx1024M -jar paper.jar nogui > server.log 2>&1 &
          PID=$!
          OK=0
          for i in $(seq 1 130); do
            if grep -Fq "LegendaryRais v3.4.0 ULTIMATE ARSENAL enabled." server.log; then OK=1; break; fi
            if grep -Fq "Error occurred while enabling LegendaryRais" server.log; then break; fi
            if ! kill -0 "$PID" 2>/dev/null; then break; fi
            sleep 1
          done
          kill "$PID" 2>/dev/null || true
          wait "$PID" 2>/dev/null || true
          grep -i -C 14 LegendaryRais server.log || true
          test "$OK" -eq 1
      - uses: actions/upload-artifact@v4
        with:
          name: LegendaryRais-3.4.0-ULTIMATE-ARSENAL-PAPER-26.1.2
          path: |
            LegendaryRais-3.4.0-ULTIMATE-ARSENAL-PAPER-26.1.2.jar
            LegendaryRais-3.4.0-ULTIMATE-ARSENAL-PAPER-26.1.2.sha256
          retention-days: 7
      - name: Publish verified plugin
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add LegendaryRais-3.4.0-ULTIMATE-ARSENAL-PAPER-26.1.2.jar LegendaryRais-3.4.0-ULTIMATE-ARSENAL-PAPER-26.1.2.sha256
          git commit -m "Publish LegendaryRais v3.4 Ultimate Arsenal plugin" || exit 0
          git reset --hard HEAD
          git clean -fd
          git pull --rebase origin main
          git push
